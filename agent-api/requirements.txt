fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.28.0
redis[hiredis]==5.2.0
anthropic==0.40.0
langfuse==2.36.2
prometheus-client==0.21.1
prometheus-fastapi-instrumentator==7.0.0
pydantic-settings==2.6.1
python-dotenv==1.0.1
aiosqlite==0.20.0
aiofiles==24.1.0
jsonschema==4.25.1
eval-type-backport==0.2.0
PyYAML==6.0.2
aiomysql==0.2.0
asyncpg>=0.29.0
pyjwt>=2.8.0
pymupdf==1.24.13
reportlab==4.2.5
python-multipart==0.0.20

# Optional OCR fallback for raster-image inputs (PNG/JPEG). The tesseract
# binary itself must be present in the deploy image; without it
# documents/ocr.py falls back to a single page-level block with confidence
# 0.0 and the critic degradation path soft-warns.
pytesseract==0.3.13
Pillow>=10.0.0

# Wave 2A: PaddleOCR as an alternate OCR engine, gated by the ``OCR_ENGINE``
# env var (default ``tesseract``). CPU-only wheels — never use the GPU
# variants in this image. paddleocr 2.8.x is the latest stable that pairs
# with paddlepaddle 2.6.x. Both are heavyweight (~600MB combined) so the
# adapter defers ``import paddleocr`` until first use.
paddlepaddle==2.6.2
paddleocr==2.8.1
# paddlepaddle imports `setuptools` at runtime (paddle.utils.cpp_extension);
# python:3.12-slim does not ship it, so pin it explicitly.
setuptools>=68.0.0

# Phase 3 Wave 2A — photo preprocessing pipeline (deskew / perspective unwarp
# / CLAHE / bilateral denoise) pre-OCR. Headless build (no GTK / Qt deps);
# runtime libs (libgl1, libglib2.0-0) already in the Dockerfile for paddleocr.
# Gated by ``PHOTO_PREPROCESS={off,auto,force}`` — default off, so cold-start
# cost is paid only when the flag is enabled.
opencv-python-headless==4.10.0.84

# Phase 3 Wave 2B — polygon-vs-polygon IoU for the citation_iou rubric.
# Optional at runtime: rubrics_mechanical falls back to a pure-Python
# Sutherland-Hodgman + shoelace path when shapely isn't installed (so
# isolated test runs without the wheel still pass).
shapely>=2.0,<3

# Week-2 LangGraph skeleton (Slice 3.1+).
langgraph==0.2.60
langchain-core==0.3.28

# Hybrid-RAG (W2 §6 / Slices 4.1-4.4): pgvector text codec, deterministic
# offline embedding stub (numpy), Voyage embeddings, Cohere rerank.
cohere==5.13.2
voyageai==0.3.2
pgvector==0.3.6
numpy==1.26.4

# Phase 9 Slice 9.4 — HL7 v2 parser (ADT^A08, ORU^R01). MIT, pure-Python.
# Pinned to 1.3.5 to keep grammar-table behaviour stable across deploys.
hl7apy==1.3.5

# Dev / lint (no separate requirements-dev.txt exists yet)
import-linter==2.5.2
pytest-asyncio==0.24.0
